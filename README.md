# RP2040 Development Flake
This flake contains packages for working with the RPi Pico.

## Usage
Initialize a Pico SDK project;
```sh
nix flake init --template github:paperdev-code/rp2040-flake
direnv allow
```
